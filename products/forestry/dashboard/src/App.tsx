// products/forestry/dashboard/src/App.tsx (placeholder — Task 8 replaces this)

// Declared here verbatim from the source branch so MapView.tsx type-checks
// before Task 8 lands the full App.tsx port, which re-declares it identically.
export interface ZoomRequest {
  featureOrdinal: number
  nonce: number
}

export default function App() {
  return <div>Forestry demo — under construction</div>
}
